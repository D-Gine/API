# D&Gine API

## Build and Run with

For this project you need the following environment variables:
```
# DATABASE
DB_HOST=dengine-postgres
DB_PORT=5432
DB_USER=[USER]
DB_PWD=[PWD]
DB_NAME=dengine

# API
API_PORT=8080
GIN_MODE=release
TOKEN_KEY=[KEY]
```