services:
  dengine-api:
    container_name: dengine-api-${VERSION}
    build: ./api
    env_file: ".env"
    ports:
      - "${API_PORT}:${API_PORT}"
    networks:
      - back

networks:
  back:
    external: true
    name: ${BACK_NETWORK}
